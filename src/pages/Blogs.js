import React, { Fragment } from 'react';
import Blogs from '../containers/Blogs';

const BlogPage = () => {
  return (
    <Fragment>
      <Blogs />
    </Fragment>
  );
};

export default BlogPage;
